Run `./agent-build.sh` before sharing code changes, using `with_escalated_permissions: true`. Anything other than complete silence and an exit code of 0 is considered a failure. There is a full log available for debugging purposes only, at `build/latest.log`.
